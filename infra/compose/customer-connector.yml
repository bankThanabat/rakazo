services:
  connector:
    image: rakazo-openconnector:${OPENCONNECTOR_SOURCE_ID:?Prepare the locked connector source}
    pull_policy: never
    networks:
      default: {}
      support:
        aliases: [rakazo-support-connector]
networks:
  support:
    external: true
    name: rakazo-support
